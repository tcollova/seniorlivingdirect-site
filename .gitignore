# OS junk
Thumbs.db
desktop.ini
.DS_Store

# Editor scratch
*.swp
*~

# Netlify CLI local state (created by `netlify link`)
.netlify/

# Internal strategy/working docs — must NOT be served publicly
decisions_log.md
changes_checklist.txt
portfolio_removal_summary.md
project_brief.md
project_setup.md
brand_assets.md
voice_guide.md
portfolio_reference.md
files.zip
/files/

# Markdown/text docs don't belong in /images/
images/*.md
images/*.txt

# Internal HTML reference snippets — not a real page
seo-head-snippets.html

# All Markdown is internal by default (blog posts on this site are HTML).
# readme.md was added to the repo before this rule and stays tracked.
*.md
